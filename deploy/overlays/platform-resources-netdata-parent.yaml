# Netdata parent memory request tune — helm upgrade netdata ... --reuse-values -f this file
parent:
  resources:
    requests:
      cpu: 100m
      memory: 192Mi
    limits:
      cpu: 500m
      memory: 1Gi
